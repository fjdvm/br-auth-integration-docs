# Centralized Auth Integration Docs

The published GitHub Pages guide is in [index.md](./index.md). It documents Portal, HRMS, POS, SCMS, and OOS integration with the Centralized Auth Service.

GitHub Pages is deployed automatically from `main` by `.github/workflows/deploy-pages.yml`.
